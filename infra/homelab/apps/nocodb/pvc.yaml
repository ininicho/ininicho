apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nocodb-data
  namespace: nocodb
spec:
  storageClassName: seagate-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

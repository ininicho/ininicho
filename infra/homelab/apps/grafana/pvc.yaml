apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: monitoring
spec:
  storageClassName: seagate-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

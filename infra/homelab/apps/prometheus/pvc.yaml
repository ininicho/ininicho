apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-data
  namespace: monitoring
spec:
  storageClassName: seagate-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
